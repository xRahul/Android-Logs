<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Base Compose Application Theme -->
    </style>

    <style name="LogsTheme" parent="android:Theme.Material" />

</resources>
